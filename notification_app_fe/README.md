# Notification App (frontend)

Lightweight React + Vite skeleton using Material UI. This is a minimal starting point for Stage 2.

Run (from `notification_app_fe`):

```bash
npm install
npm run dev
```

The app expects the notifications API to be reachable; set the API base URL in `src/config.js` or call the API directly from the UI.
