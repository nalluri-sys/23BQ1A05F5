# Notification System Design

This file describes the notification system design for the frontend implementation.

- Priority inbox: combine type weight and recency to compute top-N.
- Frontend: `notification_app_fe` directory contains the React app.
- Backend placeholder: `notification_app_be` (empty placeholder for stage 2 back-end work).
- Logging middleware: `logging_middleware` folder for middleware implementation.
